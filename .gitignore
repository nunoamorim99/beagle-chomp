node_modules
dist
dist-ssr
# IDEA-051: the metrics portal build — its own Cloudflare Pages project
dist-admin
dev-dist
*.local
.DS_Store
.vite

# Backend (server/)
server/node_modules
server/dist
# Game modules copied from src/game at build time by
# server/scripts/sync-game-constants.ts — generated, never edited by hand.
server/vendor

# Secrets — never commit these (STACK.md §2.6). .env.example IS committed.
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example
.tmp-screens/

# img2threejs skill workspace (reference imagery + pipeline state) — never committed.
.img2threejs/

# img2threejs pipeline: regenerable derivatives (the spec, evidence, state and
# per-pass review renders are kept; camera sweeps, zone crops and the HUD
# diagnostics are not)
.img2threejs/renders/sweep/
.img2threejs/renders/diag.png
.img2threejs/detail-zones/
*.ts.bak

# IDEA-062: the editor writes a one-generation backup beside every file it
# overwrites (vite.config.ts, dev-only). Never committed.
*.editorbak

# IDEA-052b: VAPID keypairs are secrets — they belong in Dokploy env vars,
# never in the repo. Generated with: npx web-push generate-vapid-keys
vapid*.json
vapid_*.txt
