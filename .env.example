# Frontend build-time config. Copy to .env for local dev.
# NOT a secret — this ends up in the client bundle by design; it's a public URL.
# Real secrets live only in the API's Dokploy env vars (STACK.md §2.6).
VITE_API_URL=http://localhost:3001
